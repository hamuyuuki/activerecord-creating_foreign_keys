default: &default
  database: test

test:
  <<: *default
  adapter: sqlite3
  pool: 5
  timeout: 5000
  database: db/test.sqlite3

mysql:
  <<: *default
  adapter: mysql2
  encoding: utf8
  username: root
  password: root
  host: <%= ENV["CI"] ? "localhost" : "mysql" %>
